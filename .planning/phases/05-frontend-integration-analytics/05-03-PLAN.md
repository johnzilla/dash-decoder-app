---
phase: 05-frontend-integration-analytics
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
  - src/App.tsx
  - src/components/PrivacyBanner.tsx
autonomous: true
requirements: [ANLYT-05, EXPT-03]

must_haves:
  truths:
    - "Plausible Analytics script is loaded and records page views"
    - "Privacy notice is visible on all pages disclosing AI processing and data collection"
  artifacts:
    - path: "index.html"
      provides: "Plausible Analytics script tag"
      contains: "plausible"
    - path: "src/components/PrivacyBanner.tsx"
      provides: "Persistent privacy notice footer banner"
      exports: ["PrivacyBanner"]
  key_links:
    - from: "src/App.tsx"
      to: "src/components/PrivacyBanner.tsx"
      via: "renders PrivacyBanner on all pages"
      pattern: "<PrivacyBanner"
---

<objective>
Add Plausible Analytics for traffic tracking and a privacy notice banner so users know about AI processing and anonymous data collection.

Purpose: Plausible captures page views and UTM params from Google Ads to measure traffic. Privacy notice provides transparency per EXPT-03.
Output: Plausible script in index.html, PrivacyBanner component rendered on all pages via App.tsx.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-frontend-integration-analytics/05-CONTEXT.md

<interfaces>
<!-- Current index.html structure -->
```html
<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <link rel="icon" type="image/x-icon" href="/favicon.ico" />
    <link rel="apple-touch-icon" href="/apple-touch-icon.png" />
    <meta name="theme-color" content="#2563EB" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>DashDecoder</title>
  </head>
  <body>
    <div id="root"></div>
    <script type="module" src="/src/main.tsx"></script>
  </body>
</html>
```

<!-- Current App.tsx structure -->
```typescript
export default function App() {
  return (
    <BrowserRouter>
      <ScanProvider>
        <div className="min-h-screen bg-background text-foreground">
          <Suspense fallback={<Loading />}>
            <Routes>
              <Route path="/" element={<Home />} />
              <Route path="/scan" element={<Scan />} />
              <Route path="/results" element={<Results />} />
            </Routes>
          </Suspense>
        </div>
      </ScanProvider>
    </BrowserRouter>
  );
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Plausible Analytics script tag</name>
  <files>index.html</files>
  <read_first>index.html, .planning/phases/05-frontend-integration-analytics/05-CONTEXT.md</read_first>
  <action>
Per D-08 and ANLYT-05: Add the Plausible Analytics script tag to the &lt;head&gt; section of index.html, after the existing meta tags.

Add this exact script tag:
```html
<script defer data-domain="dashdecoder.com" src="https://plausible.io/js/script.js"></script>
```

This is the standard Plausible embed — zero React code needed. It automatically tracks page views and captures UTM parameters (utm_source, utm_medium, utm_campaign) from Google Ads traffic.

Use "dashdecoder.com" as the data-domain value. If the production domain differs, the executor should use the actual production domain from .do/app.yaml or adjust after deployment. This is a Claude's Discretion item per CONTEXT.md.

Do NOT add any Plausible React wrapper, custom events, or SPA integration — the basic script handles page views with browser history API out of the box.
  </action>
  <verify>
    <automated>cd /home/john/vault/projects/github.com/dash-decoder-app && grep -c "plausible" index.html</automated>
  </verify>
  <acceptance_criteria>
    - index.html contains: script defer data-domain
    - index.html contains: plausible.io/js/script.js
    - index.html contains: data-domain="dashdecoder.com"
    - No Plausible-related imports in any .tsx file
  </acceptance_criteria>
  <done>Plausible Analytics script tag in index.html head, tracks page views and UTM params automatically</done>
</task>

<task type="auto">
  <name>Task 2: Create PrivacyBanner component and add to App.tsx</name>
  <files>src/components/PrivacyBanner.tsx, src/App.tsx</files>
  <read_first>src/App.tsx, .planning/phases/05-frontend-integration-analytics/05-CONTEXT.md</read_first>
  <action>
1. Create src/components/PrivacyBanner.tsx per D-10 and D-11:

   A small, persistent footer banner visible on all pages. Non-intrusive, always visible (no dismiss button — per D-10 "persistent").

   Content text (per D-11, friendly plain English):
   "We use AI to analyze your photo. We collect anonymous usage data to improve the app. No personal info is stored."

   Implementation:
   - Fixed to bottom of viewport: className="fixed bottom-0 left-0 right-0"
   - Small and subtle: py-2 px-4, text-xs, bg-muted/90 backdrop-blur-sm border-t
   - Text centered, text-muted-foreground
   - z-index high enough to stay above page content: z-50
   - Export as named export: export function PrivacyBanner()
   - No state, no props, pure presentational component

2. Add to src/App.tsx:
   - Import PrivacyBanner: import { PrivacyBanner } from '@/components/PrivacyBanner'
   - Render inside the outer div, after Suspense block (sibling, not child):
   ```
   <div className="min-h-screen bg-background text-foreground">
     <Suspense fallback={<Loading />}>
       <Routes>...</Routes>
     </Suspense>
     <PrivacyBanner />
   </div>
   ```
   - Add pb-10 to the outer div className to prevent page content from being hidden behind the fixed banner: "min-h-screen bg-background text-foreground pb-10"
  </action>
  <verify>
    <automated>cd /home/john/vault/projects/github.com/dash-decoder-app && npx tsc -b --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - src/components/PrivacyBanner.tsx exists and exports PrivacyBanner
    - PrivacyBanner.tsx contains: "We use AI to analyze your photo"
    - PrivacyBanner.tsx contains: "No personal info is stored"
    - PrivacyBanner.tsx contains: "anonymous usage data"
    - PrivacyBanner.tsx contains: fixed bottom-0 for footer positioning
    - src/App.tsx imports PrivacyBanner
    - src/App.tsx renders <PrivacyBanner
    - src/App.tsx contains: pb-10 for bottom padding
  </acceptance_criteria>
  <done>Privacy banner visible on all pages with plain English disclosure about AI processing and anonymous data collection</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> plausible.io | Third-party analytics script loaded from CDN |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-07 | Information Disclosure | Plausible script | accept | Plausible is privacy-focused (no cookies, GDPR compliant). Only captures page views and UTM params. No PII transmitted |
| T-05-08 | Tampering | Plausible CDN | accept | Standard risk for any third-party script. Plausible is a trusted vendor. SRI hash could be added but breaks on version updates |
</threat_model>

<verification>
- Plausible script tag present in index.html: grep "plausible" index.html
- Privacy banner renders on all pages: check App.tsx includes PrivacyBanner
- TypeScript compiles: npx tsc -b --noEmit
</verification>

<success_criteria>
- Plausible Analytics script loads in production and records page views
- Privacy notice visible at bottom of every page with plain English disclosure
- No new TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/05-frontend-integration-analytics/05-03-SUMMARY.md`
</output>
