---
phase: 05-frontend-integration-analytics
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - server/routes/feedback.ts
  - server/app.ts
  - src/components/feedback/FeedbackCard.tsx
  - src/routes/results.tsx
autonomous: true
requirements: [ANLYT-01, ANLYT-02]

must_haves:
  truths:
    - "After diagnosis, user sees a feedback card with 3 structured questions and a free-text field"
    - "User can submit feedback and sees a thank-you confirmation"
    - "Feedback is persisted to Postgres via POST /api/sessions/:id/feedback"
  artifacts:
    - path: "server/routes/feedback.ts"
      provides: "POST /api/sessions/:id/feedback endpoint"
      exports: ["feedbackRouter"]
    - path: "src/components/feedback/FeedbackCard.tsx"
      provides: "Feedback card UI with 3 questions + free text"
      exports: ["FeedbackCard"]
  key_links:
    - from: "src/components/feedback/FeedbackCard.tsx"
      to: "/api/sessions/:id/feedback"
      via: "fetch POST on submit"
      pattern: "fetch.*api/sessions.*feedback"
    - from: "server/routes/feedback.ts"
      to: "feedback table"
      via: "Drizzle insert"
      pattern: "db\\.insert\\(feedback\\)"
---

<objective>
Build the feedback card UI and its backend endpoint so users can rate and comment on diagnosis accuracy after receiving results.

Purpose: Captures structured user feedback (accuracy, usefulness, next action) to measure value proposition during the Google Ads experiment.
Output: FeedbackCard component rendered on results page, POST /api/sessions/:id/feedback endpoint persisting to Postgres.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-frontend-integration-analytics/05-CONTEXT.md

<interfaces>
<!-- Existing feedback table schema from server/db/schema.ts -->
```typescript
export const feedback = pgTable('feedback', {
  id: integer().primaryKey().generatedAlwaysAsIdentity(),
  sessionId: integer('session_id').notNull().references(() => scanSessions.id),
  createdAt: timestamp('created_at').notNull().defaultNow(),
  rating: integer('rating'),
  comment: text('comment'),
});
```

<!-- Results page receives diagnosis via location.state -->
```typescript
// src/routes/results.tsx
interface LocationState {
  diagnosis: Diagnosis;
}
```

<!-- Express app registers routers in server/app.ts -->
```typescript
app.use('/api/sessions', sessionsRouter);
app.use('/api/analyze', analyzeRouter);
```

<!-- Available shadcn/ui components -->
<!-- src/components/ui/: alert.tsx, button.tsx, card.tsx, input.tsx, label.tsx -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create feedback API endpoint and extend schema</name>
  <files>server/routes/feedback.ts, server/app.ts, server/db/schema.ts</files>
  <read_first>server/db/schema.ts, server/app.ts, server/routes/sessions.ts, .planning/phases/05-frontend-integration-analytics/05-CONTEXT.md</read_first>
  <action>
1. Extend the feedback table in server/db/schema.ts to support the 3 structured questions per D-01. Add columns:
   - `accuracyRating` varchar('accuracy_rating', { length: 10 }) — values: "yes", "no", "unsure"
   - `usefulnessRating` integer('usefulness_rating') — 1-5 scale
   - `nextAction` varchar('next_action', { length: 20 }) — values: "fix-myself", "mechanic", "ignore", "other"
   Keep existing `rating` and `comment` columns (comment serves as the free-text field per D-02, max 500 chars enforced server-side).

2. Create server/routes/feedback.ts with a single route:
   - POST /api/sessions/:id/feedback (per D-05)
   - Validate request body with Zod: accuracyRating (enum yes/no/unsure, required), usefulnessRating (number 1-5, required), nextAction (enum fix-myself/mechanic/ignore/other, required), comment (string max 500, optional)
   - Validate :id is a valid integer
   - Verify session exists (SELECT from scanSessions by id) before inserting — return 404 if not found
   - Insert into feedback table with all fields
   - Return 201 with { id, sessionId, createdAt }
   - Return 400 for validation errors with Zod error messages
   - Return 500 with { error: "Failed to save feedback" } on DB errors

3. Register in server/app.ts: import feedbackRouter, mount at app.use('/api/sessions', feedbackRouter) — the router handles the /:id/feedback sub-path internally. Note: mount AFTER sessionsRouter since Express matches first registered. Actually, to avoid conflicts, mount the feedback router's path as '/api/sessions' and define the route as '/:id/feedback' inside the router, OR mount it distinctly. The cleanest approach: in feedback.ts, export a router with route '/:id/feedback', and in app.ts mount it at '/api/sessions' (Express will match the more specific path /:id/feedback before /:id PATCH).
  </action>
  <verify>
    <automated>cd /home/john/vault/projects/github.com/dash-decoder-app && npx tsc -b --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - server/routes/feedback.ts exports feedbackRouter
    - feedback.ts contains: z.enum(["yes", "no", "unsure"]) for accuracy validation
    - feedback.ts contains: z.number().min(1).max(5) for usefulness validation
    - feedback.ts contains: z.string().max(500) for comment validation
    - feedback.ts contains: db.insert(feedback) for persistence
    - server/db/schema.ts contains: accuracy_rating column
    - server/db/schema.ts contains: usefulness_rating column
    - server/db/schema.ts contains: next_action column
    - server/app.ts imports and mounts feedbackRouter
  </acceptance_criteria>
  <done>POST /api/sessions/:id/feedback endpoint created with Zod validation and Drizzle persistence for all 3 structured fields + optional comment</done>
</task>

<task type="auto">
  <name>Task 2: Build FeedbackCard component and wire into results page</name>
  <files>src/components/feedback/FeedbackCard.tsx, src/routes/results.tsx</files>
  <read_first>src/routes/results.tsx, src/components/ui/card.tsx, src/components/ui/button.tsx, src/components/ui/label.tsx, src/components/ui/input.tsx, .planning/phases/05-frontend-integration-analytics/05-CONTEXT.md</read_first>
  <action>
1. Create src/components/feedback/FeedbackCard.tsx implementing D-01 through D-04:

   Props: { sessionId: number } (needed to POST feedback to correct session)

   Three structured questions (per D-01):
   a) "Was this diagnosis accurate?" — 3 buttons: Yes / No / Unsure (highlight selected, store as "yes"/"no"/"unsure")
   b) "How useful was this?" — 5 clickable stars (1-5 rating, filled stars up to selected value, store as number 1-5)
   c) "What will you do next?" — 4 buttons: "Fix it myself" / "Go to mechanic" / "Ignore it" / "Other" (highlight selected, store as "fix-myself"/"mechanic"/"ignore"/"other")

   Optional free-text (per D-02):
   - textarea with placeholder "Any additional comments?" and maxLength={500}
   - Character counter showing "X/500" below textarea

   Submit button: disabled until all 3 required questions answered.

   On submit (per D-05):
   - POST to `/api/sessions/${sessionId}/feedback` with JSON body { accuracyRating, usefulnessRating, nextAction, comment }
   - On success (201): transition to collapsed "Thanks for your feedback!" message (per D-04)
   - On error: show inline error message, keep form state so user can retry

   Styling: Use shadcn card.tsx wrapper, Tailwind for layout. Mobile-first. Card always visible below diagnosis (per D-03), no delay or animation gate.

   State management: local useState for each answer + submitted boolean + error string.

2. Wire into src/routes/results.tsx:
   - Import FeedbackCard from '@/components/feedback/FeedbackCard'
   - The results page needs a sessionId. For now, accept sessionId from location.state alongside diagnosis. Update LocationState interface to: { diagnosis: Diagnosis; sessionId?: number }
   - Render <FeedbackCard sessionId={sessionId} /> below <DiagnosisResult /> — only if sessionId is present
   - Do NOT change DiagnosisResult component
  </action>
  <verify>
    <automated>cd /home/john/vault/projects/github.com/dash-decoder-app && npx tsc -b --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - src/components/feedback/FeedbackCard.tsx exists and exports FeedbackCard
    - FeedbackCard.tsx contains: "Was this diagnosis accurate?" text
    - FeedbackCard.tsx contains: "How useful was this?" text
    - FeedbackCard.tsx contains: "What will you do next?" text
    - FeedbackCard.tsx contains: maxLength={500} or max(500) for comment field
    - FeedbackCard.tsx contains: fetch.*api/sessions.*feedback for POST call
    - FeedbackCard.tsx contains: "Thanks for your feedback" text for post-submit state
    - src/routes/results.tsx imports FeedbackCard
    - src/routes/results.tsx renders FeedbackCard with sessionId prop
  </acceptance_criteria>
  <done>FeedbackCard renders 3 structured questions + free text below diagnosis, submits to backend, collapses to thank-you on success</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> /api/sessions/:id/feedback | Untrusted feedback data from browser |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | feedback endpoint | mitigate | Validate all fields with Zod at route entry; reject unknown fields; enforce comment max 500 chars server-side |
| T-05-02 | Spoofing | feedback sessionId | accept | No auth in v1.1; anyone can submit feedback to any session. Low risk: feedback is anonymous analytics data, not PII |
| T-05-03 | Denial of Service | feedback endpoint | accept | Rate limiting deferred to Phase 6 (EXPT-04). Low volume expected during experiment |
</threat_model>

<verification>
- TypeScript compiles with no errors: `npx tsc -b --noEmit`
- FeedbackCard component exists with all 3 questions
- Feedback endpoint validates input with Zod
- Results page renders FeedbackCard below diagnosis
</verification>

<success_criteria>
- User sees feedback card with 3 structured questions + free text after diagnosis
- Submitting feedback persists to Postgres and shows thank-you message
- Invalid feedback payloads are rejected with 400 status
</success_criteria>

<output>
After completion, create `.planning/phases/05-frontend-integration-analytics/05-01-SUMMARY.md`
</output>
